function [pulse] = generate_neighs(options)

switch options.type
    case 'gaussian'
        
        
    otherwise 
        msg = 'this type is not available for the moment'
        error(msg)
end


end